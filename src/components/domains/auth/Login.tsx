/* eslint-disable @typescript-eslint/no-unused-vars */

type LoginProps = { depth?: number; ipAddress?: string };

export default function Login({ depth, ipAddress }: LoginProps) {
  return <div tw="flex flex-col h-full relative" />;
}
