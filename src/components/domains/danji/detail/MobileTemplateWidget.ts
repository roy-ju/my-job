import tw, { styled } from 'twin.macro';

export const Container = styled.div`
  ${tw`relative flex flex-col w-full h-full overflow-x-hidden`}
`;

export const HeaderWrraper = styled.div`
  ${tw`[z-index: 500]`}
`;
