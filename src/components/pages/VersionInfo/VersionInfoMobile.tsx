import VersionInfo from '@/components/domains/terms-and-info/VersionInfo';

import { MobileContainer } from '@/components/atoms';

export default function VersionInfoMobile() {
  return (
    <MobileContainer>
      <VersionInfo />
    </MobileContainer>
  );
}
