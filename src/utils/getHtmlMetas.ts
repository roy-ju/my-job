// import Routes from '@/router/routes';
import { ParsedUrlQuery } from 'querystring';

export default async function getHtmlMetas(_: ParsedUrlQuery) {
  // if (query.depth1 === Routes.ListingDetail) {
  //   // do stuff
  // }

  // if (query.depth1 === Routes.)

  return null;
}
